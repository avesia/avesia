# these tasks should be run in the root directory.

version: '3'

tasks:
  build-web:
    dir: avesia-web
    cmds:
      - npm install
      - npm run build

  build-web-dev:
    dir: avesia-web
    cmds:
      - npm install
      - npm run dev
